% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Harmony_decision.R
\name{Harmony_decision}
\alias{Harmony_decision}
\title{Calculate Harmony Decision Across All Taxonomic Levels}
\usage{
Harmony_decision(
  Dat,
  Homogenise_ratio,
  taxonomic_hierarchy,
  taxon_col = "nom",
  level_col = "Determination",
  count_col = "total"
)
}
\arguments{
\item{Dat}{A data frame containing taxonomic data.}

\item{Homogenise_ratio}{Numeric. The threshold ratio to determine if a taxon should be harmonized.}

\item{taxonomic_hierarchy}{Character vector. The ordered list of taxonomic levels (e.g., \code{c("Family", "Genus", "Species")}).}

\item{taxon_col}{Character. The name of the column containing the specific taxon name. Default is \code{"nom"}.}

\item{level_col}{Character. The name of the column containing the determination level. Default is \code{"Determination"}.}

\item{count_col}{Character. The name of the column containing the abundance/occurrence counts. Default is \code{"total"}.}
}
\value{
A data frame containing the results of the logical tree analysis for all levels.
}
\description{
A wrapper function for \code{TaxLogTree} that calculates the focal-offspring ratio for each taxon across the entire provided taxonomic hierarchy.
}
\examples{
tax_data <- data.frame(
  Family = c("Baetidae", "Baetidae", "Baetidae"),
  Genus = c("Baetis", "Baetis", NA),
  Species = c("Baetis fuscatus", NA, NA),
  nom = c("Baetis fuscatus", "Baetis", "Baetidae"),
  Determination = c("Species", "Genus", "Family"),
  total = c(10, 5, 2)
)
hierarchy <- c("Family", "Genus", "Species")
Harmony_decision(tax_data, 0.5, hierarchy)
}
