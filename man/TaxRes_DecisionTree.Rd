% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TaxRes_DecisionTree.R
\name{TaxRes_DecisionTree}
\alias{TaxRes_DecisionTree}
\title{Apply Taxonomic Resolution Decision Tree}
\usage{
TaxRes_DecisionTree(
  Dat,
  taxonomic_hierarchy,
  TaxLogTree_results,
  ConservationDegree = "HT_test"
)
}
\arguments{
\item{Dat}{A data frame containing taxonomic data.}

\item{taxonomic_hierarchy}{Character vector of the ordered taxonomic levels.}

\item{TaxLogTree_results}{Data frame. The output from \code{TaxLogTree} or \code{Harmony_decision}.}

\item{ConservationDegree}{Character. The column name in \code{TaxLogTree_results} containing the logical decision. Default is \code{"HT_test"}.}
}
\value{
The original data frame appended with \code{Decision} and \code{Updated_taxon} columns.
}
\description{
Maps the logical results from \code{TaxLogTree} back to the dataset to assign \code{Keep} or \code{Delete} decisions to rows.
}
\examples{
tax_data <- data.frame(
  Family = c("Baetidae", "Baetidae", "Baetidae"),
  Genus = c("Baetis", "Baetis", NA),
  Species = c("Baetis fuscatus", NA, NA),
  nom = c("Baetis fuscatus", "Baetis", "Baetidae"),
  Determination = c("Species", "Genus", "Family"),
  total = c(10, 5, 2)
)
hierarchy <- c("Family", "Genus", "Species")
Harmony_decision(tax_data, 0.5, hierarchy)
Harmony_results <- Harmony_decision(tax_data, 0.5, hierarchy)
TaxRes_DecisionTree(
 Dat = tax_data,
 taxonomic_hierarchy = hierarchy,
 TaxLogTree_results = Harmony_results,
 ConservationDegree = "HT_test"
)
}
